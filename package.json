{
  "name": "sanity-nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prepare": "cd studio && yarn install",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "studio": "cd studio && yarn start",
    "prod-build": "cd studio && yarn install && yarn sanity build ../public/studio -y && cd .. && next build"
  },
  "dependencies": {
    "next": "12.1.6",
    "react": "18.1.0",
    "react-dom": "18.1.0"
  },
  "devDependencies": {
    "eslint": "8.15.0",
    "eslint-config-next": "12.1.6"
  }
}
